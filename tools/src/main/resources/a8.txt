SELECT dt, management_city_id, business_group_type, adjust_type, sku_id_or_category_id, theory_sale_sum_sum
FROM mart_forecast.forecast_theory_sale
-- 同步一天
-- where dt = '$now.datekey'
-- 历史刷数 去年 前年 今天 前后扩展多少天
-- where (dt BETWEEN 20230624 and 20231103 ) or (dt BETWEEN 20240624 and 20241103 ) or (dt BETWEEN 20250624 and 20250912 )
-- 同步昨天  去年今日加50天  前年今日加50天
where dt = '$now.datekey' or dt = '$now.delta(years=1).delta(days=-50).datekey' or dt = '$now.delta(years=2).delta(days=-50).datekey'

;
